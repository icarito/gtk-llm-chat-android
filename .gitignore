node_modules/
*-service-account*.json
*firebase-admin*.json
*fcm*.json
.expo/
dist/
web-build/
# android/ SE VERSIONA: este proyecto tiene código nativo propio (el módulo
# XMPP y su foreground service), así que no puede regenerarse entero desde
# app.json. Ignoramos sólo lo que produce el build.
android/.gradle/
android/.idea/
android/build/
android/app/build/
android/local.properties
android/app/google-services.json
android/gradle.properties
# Keystore de depuración: lo regenera el SDK, y ningún keystore va a git.
android/app/debug.keystore
*.orig.*
*.jks
*.p8
*.p12
*.key
*.mobileprovision
.env
.env.local
.env*.local
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.DS_Store
Thumbs.db

# Fastlane / builds nativas
build/
fastlane/report.xml
fastlane/Preview.html
fastlane/test_output
*.ipa
*.apk
vendor/bundle/
.bundle/

expo-env.d.ts
